{% extends 'base.html.twig' %}

{% block title %}Auteur{% endblock %}

{% block body %}
    <style>
        .show{
            display: flex;
            align-items: center;
            justify-content: center;
        }
        form{
            margin-top: 15px;
        }
        .btn{
            margin-left: 5px;
        }
    </style>
    <h1 class="text-center mt-5">Auteur</h1>
<div class="container  mt-5">
    <table class="table">
        <tbody>
             <tr class="table-active">
                 <th class="table-warning">Id</th>
                <td>{{ auteur.id }}</td>
            </tr>
             <tr class="table-active">
                <th class="table-warning">Nom</th>
                <td>{{ auteur.fullname }}</td>
            </tr>
        </tbody>
    </table>

    <div class="show">
        <a class="btn btn-success" href="{{ path('auteur_index') }}">Retour à list</a>

        <a class="btn btn-primary" href="{{ path('auteur_edit', {'id': auteur.id}) }}">edite</a

        <a class="del">{{ include('auteur/_delete_form.html.twig') }}</a>
    </div>
</div>
{% endblock %}
