{% extends 'base.html.twig' %}
{% block body %}
    <style>
        .wrap{
            background: #3D566E;
            border-radius: 5%;
            padding: 2rem 0 2em 5rem ;
            margin: 60px 220px;
        }
        input[type="text"],input[type="password"]{
            width: 80%;
        }

    </style>
    <div class="container">
        <div class="row">
             {% if error %}
                  <div class="alert alert-danger">
                      {{ error.messageKey|trans(error.messageData, 'security') }}
                  </div>
            {% endif %}
        </div>
        <div class="row">
             {% if app.user %}
                You are logged in as {{ app.user.username }},
                <a href="{{ path('app_logout') }}">Logout</a>
              {% endif %}
        </div>
        <div class="wrap">


            <form action="{{ path('app_login') }}" method="post">
                <div class="form-group">
                    <label for="exampleInputEmail1">Email address</label>
                    <input type="text" value="{{ last_username }}" name="username" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="Enter email">
{#                    <small id="emailHelp" class="form-text text-muted">We'll never share your email with anyone else.</small>#}
                </div>
                <div class="form-group">
                    <label for="exampleInputPassword1">Password</label>
                    <input type="password" name="password" id="Password" class="form-control"  placeholder="Password">
                </div>

                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}" />
                <button type="submit" class="btn btn-primary">Se Connecter
                </button>
                <a class="btn btn-primary" role="button" href="{{ path('security_registration') }}">inscription</a>
            </form>


{#            <form action="{{ path('app_login') }}" method="post" class="login100-form validate-form">#}
{#                <span class="login100-form-title">#}
{#                    Member Login#}
{#                </span>#}
{#                <div class="wrap-input100 validate-input" data-validate = "Valid email is required: ex@abc.xyz">#}
{#                    <input class="input100" type="text" value="{{ last_username }}" name="username" id="username" class="form-control" required autofocus />#}
{#                     <span class="focus-input100"></span>#}
{#                     <span class="symbol-input100">#}
{#                        <i class="fa fa-envelope" aria-hidden="true"></i>#}
{#                     </span>#}
{#                </div>#}
{#                <div class="wrap-input100 validate-input" data-validate = "Password is required">#}
{#                    <input class="input100" type="password" name="password" id="Password" class="form-control" required />#}
{#                    <span class="focus-input100"></span>#}
{#                    <span class="symbol-input100">#}
{#                        <i class="fa fa-lock" aria-hidden="true"></i>#}
{#                    </span>#}
{#                </div>#}
{#                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}" />#}
{#                <div class="container-login100-form-btn">#}
{#                    <button class="login100-form-btn" type="submit">#}
{#                        Se Connecter#}
{#                    </button>#}
{#                </div>#}
{#                <a class="btn btn-primary" role="button" href="{{ path('security_registration') }}">inscription</a>#}
{#            </form>#}
        </div>
    </div>
{% endblock %}

